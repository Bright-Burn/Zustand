import { CommentsHeader, CommentsMain } from '../../components';

export function TodoPage() {

    return (
        <div className='container'>
            <CommentsHeader />
            <CommentsMain />
        </div>
    );
}